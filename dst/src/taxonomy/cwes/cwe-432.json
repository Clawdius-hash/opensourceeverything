{
  "id": "CWE-432",
  "name": "Dangerous Signal Handler not Disabled During Sensitive Operations",
  "status": "filled",
  "description": "A signal handler shares state with other handlers but is not properly masked or disabled during sensitive operations, allowing an attacker to send signals that corrupt shared state by interrupting the first handler before completion.",
  "severity": "medium",
  "category": "concurrency",
  "detection_pattern": "CONTROL node registers signal handler that accesses shared STORAGE state but does not mask signals during execution, allowing second signal to interrupt and corrupt state",
  "graph_query": "CONTROL(signal_handler)→STORAGE(shared_state) without CONTROL(signal_masking_during_handler)",
  "owasp_top10": null,
  "sans_top25": false,
  "related_cwes": ["CWE-364"],
  "references": ["https://cwe.mitre.org/data/definitions/432.html"],
  "filled_by": "night_agent",
  "filled_at": "2026-03-24T00:35:00Z"
}
